---
phase: "01-v2-features"
plan: "02"
title: "Custom Plan Builder UI (PlansTab) — create, edit, reorder exercises"
wave: 2
depends_on: ["01-01-PLAN.md"]
skills_used:
  - vercel-react-best-practices
cross_phase_deps: []
must_haves:
  truths:
    - "User can create a new custom plan with name, tag, and 1+ exercises"
    - "User can edit an existing custom plan (rename, change tag, add/remove/reorder/edit exercises)"
    - "User can delete a custom plan with confirmation"
    - "User can switch planMode to 'custom' via PlansTab UI (mode selector at top of tab)"
    - "Exercise reordering uses up/down arrow buttons (NOT drag-and-drop — per research §2)"
    - "All custom plan IDs use 'c_' prefix; all custom exercise IDs use 'ex_' prefix — generated via customPlans.js helpers"
    - "When planMode='custom' and customPlans has entries, Today tab renders the first custom plan as the active workout (basePlan derivation from Plan 01 handles this — no changes here)"
    - "PlansTab also shows non-custom mode selector (Plan A 4-day, Plan B 3-day, Custom) so the entire plan-mode UX lives in one tab"
    - "Header pill on root App still reflects planMode label correctly"
    - "Existing logs and workoutVariants remain untouched by any plan mutation"
  artifacts:
    - "src/PlansTab.jsx — full implementation: list view, edit view, mode selector, all CRUD interactions, inline styles only, max width inherited"
  key_links:
    - "PlansTab imports makeCustomPlanId, makeCustomExerciseId, CUSTOM_PLAN_TAGS from './customPlans.js' (created in Plan 01)"
    - "PlansTab consumes props from App.jsx: planMode, setPlanMode, customPlans, setCustomPlans, showToast"
    - "No edits to App.jsx in this plan — App-side wiring already done in Plan 01"

objective: |
  Build the custom workout plan creation/editing UI inside PlansTab.jsx. This is the user-facing fulfillment of REQ-11. The component is fully self-contained — App.jsx props were established in Plan 01.

context:
  - "@.vbw-planning/phases/01-v2-features/01-RESEARCH.md — section 2 (Custom Workout Plan Builder UI Patterns): full UI spec including up/down arrow reorder rationale, minimal builder UI shape, exercise row layout, advanced fields toggle"
  - "@src/App.jsx — lines 257-260 (ls helper — DO NOT call directly from PlansTab, all persistence flows through props), 254-255 (TAG_BG/TAG_FG color maps — reuse for tag picker), 389-409 (Btn component — reuse for all buttons), 412-428 (IconBtn — reuse for up/down/delete), 1040-1051 (Section/SegControl — reuse for form layout)"
  - "@.vbw-planning/REQUIREMENTS.md — REQ-11 (User-created custom workout plans, add/edit/reorder exercises, must-have)"
  - "Rationale: research §2 specifies up/down arrows for touch usability; new file means no merge conflict with Plan 03 working on ProgressTab.jsx in parallel"

tasks:
  - id: "T01"
    description: "Reimplement src/PlansTab.jsx with three view modes managed by local useState ('list' | 'edit' | 'mode'). Mode selector at top: 3 segmented buttons (Plan A 4-day, Plan B 3-day, Custom) showing current planMode, taps call setPlanMode. Below selector, when planMode !== 'custom': show subtle hint 'Switch to Custom to build your own plan'. When planMode === 'custom': show list view by default. List view: header 'Custom plans' + '+ New plan' button; map customPlans to rows showing plan name, tag pill, exercise count, [Edit] and [Delete] icon buttons per row. Empty state: 'No custom plans yet. Tap New plan to start.' Reuse Btn (color='primary' for New plan) and IconBtn for row actions. Inline styles only; no CSS. Max width inherited from App container."
    files: ["src/PlansTab.jsx"]
    acceptance: "Tab renders 3 mode buttons reflecting planMode. Selecting Custom shows list view. Empty state visible when customPlans=[]. With seeded customPlans, list rows show name, tag pill (using TAG_BG/TAG_FG), exercise count."

  - id: "T02"
    description: "Implement edit view in PlansTab.jsx. When user taps '+ New plan' OR Edit on a row, local state editingPlan = {id, name, tag, exercises[]} (clone of row or fresh blank with id from makeCustomPlanId()). Render: back arrow + 'New plan' / 'Edit plan' header; name text input; tag selector (5 buttons for CUSTOM_PLAN_TAGS, active uses TAG_FG color); exercises list with one row per exercise; '+ Add exercise' button at bottom appends {id:makeCustomExerciseId(), name:'', sets:'3', reps:'8-12', note:'', sub:null, caution:null, bench:false}; bottom action row: [Cancel] [Save]. Save: if existing id in customPlans, replace; else append. Then setCustomPlans + showToast('Plan saved','success') + return to list view. Cancel: discard editingPlan + return to list."
    files: ["src/PlansTab.jsx"]
    acceptance: "Tapping +New plan opens edit view with blank form and pre-generated c_ ID. Saving with non-empty name persists to customPlans and returns to list. Cancel returns without saving. Editing existing row pre-fills the form."

  - id: "T03"
    description: "Implement exercise row sub-component within PlansTab.jsx (inline function or separate). Row layout: name text input (flex:1), sets input (width 50px), reps input (width 70px), up arrow IconBtn (ti-arrow-up, disabled if idx===0), down arrow IconBtn (ti-arrow-down, disabled if idx===exercises.length-1), delete IconBtn (ti-x, color='cross'). Up moves exercise at idx to idx-1 (splice); Down moves to idx+1; Delete removes. All three mutate editingPlan.exercises array via setEditingPlan. Inputs update editingPlan.exercises[idx][field]. Per research §2, no drag-and-drop."
    files: ["src/PlansTab.jsx"]
    acceptance: "Each exercise row shows name + sets + reps inputs and 3 action buttons. Up/down reorder works on a 3-exercise plan (verify first row's up is disabled, last row's down is disabled). Delete removes the row. All edits stay in editingPlan local state until Save."

  - id: "T04"
    description: "Implement Advanced toggle in exercise row. Default: row shows just name/sets/reps + action buttons. Add small 'Advanced' Btn (color='ghost', fontSize:11) below the main row. Tapping expands an inline panel with: note textarea, caution selector (None/yellow/red as SegControl), bench checkbox, sub dropdown (null / 'bench' / 'rear_delt' / 'oh_tri'). State: openAdvanced = exerciseId or null in PlansTab. Reuse Section + SegControl from App.jsx (import them via App.jsx — if not exported, inline minimal equivalents in PlansTab — verify Section/SegControl are not currently exported; if not, duplicate locally with same shape to keep PlansTab self-contained)."
    files: ["src/PlansTab.jsx"]
    acceptance: "Advanced button visible per exercise. Tapping toggles inline panel showing note + caution + bench + sub. Values persist in editingPlan and survive save/reload."

  - id: "T05"
    description: "Implement delete confirmation: tapping Delete on a list row shows inline confirmation strip ('Delete plan [name]? [Cancel] [Delete]') in red. Confirm calls setCustomPlans(prev => prev.filter(p=>p.id !== id)) + showToast('Plan deleted','success'). Cancel hides strip. Track via local deletingId state. Also: validate Save in edit view — if editingPlan.name.trim() === '', show inline warning 'Name required' and disable Save button; if editingPlan.exercises.length === 0, show inline warning 'Add at least one exercise' and disable Save."
    files: ["src/PlansTab.jsx"]
    acceptance: "Delete shows confirmation strip; confirming removes the plan and toasts. Save button disabled when name empty or exercises empty. Validation hints visible."

verification:
  commands:
    - "npm run build  # PlansTab compiles, no missing imports"
    - "npm run dev    # manually verify: create a plan with 3 exercises, reorder, save, edit, delete with confirmation"
  grep_checks:
    - "grep -n 'makeCustomPlanId\\|makeCustomExerciseId' src/PlansTab.jsx  # confirm helpers used"
    - "grep -n 'CUSTOM_PLAN_TAGS' src/PlansTab.jsx  # confirm tag list used"
    - "grep -n 'ti-arrow-up\\|ti-arrow-down' src/PlansTab.jsx  # confirm up/down reorder UI"
    - "! grep -n '@dnd-kit\\|react-dnd\\|drag-and-drop' src/PlansTab.jsx  # confirm no DnD dependency"

success_criteria:
  - "User can switch planMode to 'custom' via PlansTab"
  - "User can create a custom plan with name, tag, and 1+ exercises"
  - "User can reorder exercises with up/down arrow buttons"
  - "User can edit an existing custom plan"
  - "User can delete a custom plan with confirmation"
  - "All custom plan/exercise IDs use c_ / ex_ prefix"
  - "Plan persists across reload (already covered by Plan 01's useEffect)"
  - "REQ-11 fulfilled"
---

# Plan 02: Custom Plan Builder UI

PlansTab now implements REQ-11 — user-created custom workout plans with reorderable exercises.

**Key directives:**
- File-disjoint with Plan 03 (only edits PlansTab.jsx; Plan 03 only edits ProgressTab.jsx)
- Up/down arrow reorder per research §2 (touch-friendly, zero new deps)
- All IDs via Plan-01-created customPlans.js helpers (enforces 'c_' / 'ex_' prefix convention)
- Reuse Btn, IconBtn, Section, SegControl shapes — keep visual consistency with existing tabs
