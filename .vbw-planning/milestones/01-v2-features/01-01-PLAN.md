---
phase: "01-v2-features"
plan: "01"
title: "Schema v3 migration, planMode, custom plans data layer, tab scaffolding"
wave: 1
depends_on: []
skills_used:
  - vercel-react-best-practices
cross_phase_deps: []
must_haves:
  truths:
    - "schemaVersion bumped from 2 to 3 in buildBackup output"
    - "usePlanA boolean replaced by planMode enum ('planA' | 'planB' | 'custom') in App state and backup payload"
    - "customPlans state slice exists in App, persisted to localStorage key 'iw_custom_plans', included in buildBackup data"
    - "migrateSchema() runs once at module scope before App renders; idempotent on repeat boots"
    - "restoreBackup handles both v2 (usePlanA) and v3 (planMode + customPlans) payloads"
    - "Custom plan IDs use 'c_' prefix convention enforced at creation site"
    - "Two new placeholder tabs ('plans' and 'progress') wired into TABS array and tab-render switch, each rendering a minimal stub component until plans 02/03 fill them"
    - "All v1 features still functional: Today/Week/Review/Check-in tabs unchanged in behavior; existing logs/workoutVariants untouched"
  artifacts:
    - "src/App.jsx — migrateSchema() at module scope, planMode state replaces usePlanA, customPlans state slice, schemaVersion: 3 in buildBackup, restoreBackup v2→v3 compat branch, TABS array extended with 'plans' + 'progress' entries, render switch dispatches to PlansTab + ProgressTab stubs"
    - "src/PlansTab.jsx — exports PlansTab component (minimal stub: renders header 'Custom plans' + 'Coming in next plan' placeholder). Wave 2 Plan 02 will replace the body."
    - "src/ProgressTab.jsx — exports ProgressTab component (minimal stub: renders header 'Progress' + 'Coming in next plan' placeholder). Wave 2 Plan 03 will replace the body."
    - "src/customPlans.js — exports helpers: makeCustomPlanId() (returns 'c_' + Date.now()), makeCustomExerciseId() (returns 'ex_' + Date.now() + '_' + randInt), CUSTOM_PLAN_TAGS array (['PUSH','PULL','LEGS','UPPER','ARMS'])"
  key_links:
    - "App.jsx imports PlansTab from './PlansTab.jsx' and ProgressTab from './ProgressTab.jsx'"
    - "App.jsx imports makeCustomPlanId, makeCustomExerciseId, CUSTOM_PLAN_TAGS from './customPlans.js' (re-exported so plans 02/03 can use them)"
    - "buildBackup signature accepts planMode and customPlans (in addition to existing args)"

objective: |
  Establish the v3 schema foundation so plans 02 (custom plan builder) and 03 (progress charts) can run in parallel without touching schema concerns. This plan handles ALL App.jsx edits for the phase — wave 2 plans add NEW files only.

context:
  - "@.vbw-planning/phases/01-v2-features/01-RESEARCH.md — section 4 (Data Architecture for v2): authoritative migration spec, planMode enum values, separate iw_custom_plans key, c_ prefix convention, restoreBackup risk #5"
  - "@src/App.jsx — lines 257-260 (ls helper), 431-453 (state slices + persistence useEffects), 469-482 (restoreBackup), 484-486 (basePlan selection — depends on usePlanA), 493-498 (TABS array), 530-548 (tab render switch), 1062-1079 (buildBackup)"
  - "@.vbw-planning/REQUIREMENTS.md — REQ-11 (custom plans must-have), REQ-15 (cloud sync — schema must support customPlans in payload)"
  - "Rationale: research recommendation #5 sequences this first; without it, plans 02/03 race on usePlanA/buildBackup shape. PlansTab/ProgressTab stubs let wave 2 work in disjoint files."

tasks:
  - id: "T01"
    description: "Add migrateSchema() function at module scope in src/App.jsx (above App component). Read iw_schema_version (default 1). If <3, read existing usePlanA (default true), write planMode key as 'planA' or 'planB', initialize iw_custom_plans to [] if missing, set iw_schema_version to 3. Leave usePlanA key in place for v2 backup-restore compat. Idempotent."
    files: ["src/App.jsx"]
    acceptance: "Function defined at module scope. Calling it twice on a fresh localStorage produces identical end state. On a v2 state (usePlanA=false), it produces planMode='planB' and customPlans=[]."

  - id: "T02"
    description: "In App component: (a) call migrateSchema() at top of component body before any useState initializers OR invoke at module scope outside component — choose module scope per research recommendation. (b) Replace usePlanA useState with planMode useState (default ls.get('planMode','planA')). (c) Add customPlans useState (default ls.get('iw_custom_plans',[])). (d) Add corresponding useEffect persistence calls (planMode → 'planMode' key; customPlans → 'iw_custom_plans' key). (e) Derive usePlanA from planMode for any consumers that still use it (or update consumers to use planMode directly). (f) Update basePlan derivation: when planMode==='custom', build basePlan from customPlans array; else keep PLAN_4/PLAN_3 selection."
    files: ["src/App.jsx"]
    acceptance: "App boots without errors. localStorage shows iw_schema_version=3 and planMode set. Switching planMode rerenders plan tabs. When planMode='custom' and customPlans=[], basePlan is empty array (UI may show empty state — that is acceptable for this plan; Plan 02 adds the builder)."

  - id: "T03"
    description: "Update buildBackup function signature and output: add planMode and customPlans to data object. Bump schemaVersion to 3. Update CheckinTab call site (line ~1129) to pass planMode and customPlans. Update buildAiExport similarly so AI export reflects v3 state (use planMode value in 'activePlan' string mapping). Update restoreBackup in App to handle v3: if data.planMode set, setPlanMode(data.planMode); else if data.usePlanA != null fall back to mapping to planA/planB. Also setCustomPlans(data.customPlans||[]). Update header pill (line ~503-507) to show planMode-aware label ('Custom' when planMode==='custom')."
    files: ["src/App.jsx"]
    acceptance: "buildBackup({...args, planMode:'custom', customPlans:[{id:'c_1',name:'Test',tag:'PUSH',exercises:[]}]}) returns schemaVersion 3 with both fields. A v2 backup file (no planMode, usePlanA:true) restored via restoreBackup sets planMode='planA'. Header pill shows 'Custom' when planMode='custom'."

  - id: "T04"
    description: "Extend TABS array in App component with two new entries: {id:'plans', icon:'ti-clipboard', label:'Plans', ac:'#A99CFF', bg:'#1B1733'} and {id:'progress', icon:'ti-chart-line', label:'Progress', ac:'#57D39A', bg:'#0F2A22'}. Move check-in icon to 'ti-heart-handshake' to free 'ti-chart-line' for progress. Add render branches in tab switch: tab==='plans' && <PlansTab planMode={planMode} setPlanMode={setPlanMode} customPlans={customPlans} setCustomPlans={setCustomPlans} showToast={showToast} /> and tab==='progress' && <ProgressTab logs={logs} checkin={checkin} macroFactor={macroFactor} weekKey={weekKey} />. Import PlansTab and ProgressTab at top of file."
    files: ["src/App.jsx"]
    acceptance: "App renders 6 tabs. Tapping Plans renders PlansTab stub. Tapping Progress renders ProgressTab stub. Existing 4 tabs continue to work unchanged."

  - id: "T05"
    description: "Create src/customPlans.js with three exports: makeCustomPlanId() returns 'c_'+Date.now(); makeCustomExerciseId() returns 'ex_'+Date.now()+'_'+Math.floor(Math.random()*10000); CUSTOM_PLAN_TAGS = ['PUSH','PULL','LEGS','UPPER','ARMS']. Create src/PlansTab.jsx exporting default PlansTab({planMode,setPlanMode,customPlans,setCustomPlans,showToast}) — minimal stub: renders div with header 'Custom plans (Plan 02 will build this)'. Create src/ProgressTab.jsx exporting default ProgressTab({logs,checkin,macroFactor,weekKey}) — minimal stub: renders div with header 'Progress charts (Plan 03 will build this)'. All three files use inline-style-only convention, max width inherited from parent. No CSS classes."
    files: ["src/customPlans.js", "src/PlansTab.jsx", "src/ProgressTab.jsx"]
    acceptance: "Files exist. App.jsx imports succeed without errors. Stubs render the placeholder text when their tab is selected."

verification:
  commands:
    - "npm run build  # must succeed with no JSX errors and no missing imports"
    - "npm run dev    # boot app, manually tap Plans + Progress tabs, verify stubs render"
  grep_checks:
    - "grep -n 'schemaVersion:3\\|schemaVersion: 3' src/App.jsx  # confirm bump"
    - "grep -n 'planMode' src/App.jsx  # confirm state introduced"
    - "grep -n 'iw_custom_plans\\|iw_schema_version' src/App.jsx  # confirm keys used"
    - "grep -n 'PlansTab\\|ProgressTab' src/App.jsx  # confirm tab wiring"

success_criteria:
  - "schemaVersion is 3 in buildBackup output"
  - "planMode replaces usePlanA across App state, persistence, backup, restore, and AI export"
  - "customPlans state slice exists, persisted, included in backup"
  - "migrateSchema runs at module load; v2→v3 path tested"
  - "PlansTab and ProgressTab stubs are reachable via tab navigation"
  - "All v1 tabs (Today/Week/Review/Check-in) function identically — manual smoke test passes"
  - "npm run build succeeds"
---

# Plan 01: Schema v3 Migration + Tab Scaffolding

This plan establishes the data shape and component surface so plans 02 (Plan Builder) and 03 (Progress Charts) can work in parallel on disjoint files.

**Key directives:**
- Module-scope `migrateSchema()` per research §4
- `c_` prefix convention enforced in `makeCustomPlanId()`
- Keep `usePlanA` localStorage key untouched for backward-compat restore
- PlansTab/ProgressTab stubs are intentional — wave 2 fills them
- All App.jsx edits for this phase happen here; wave 2 only touches new files
